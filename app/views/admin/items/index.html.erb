<h3>商品一覧</h3>
<%= link_to "新規登録", new_admin_item_path, class:"btn btn-primary" %>
<table>
<thaed>
  <tr>
    <th>商品ID</th>
    <th>商品名</th>
    <th>税抜き価格</th>
  </tr>
</thaed>
<tbody>

  <% @item.each do |item| %>
  <tr>
    <td>
      <%= item.id %>
    </td>
    <td><%= link_to item.name, admin_item_path(item.id) %></td>
    <td><%= item.price %></td>
      </tr>
  <% end %>

  </tbody>
</table>